# SPDX-FileCopyrightText: Copyright (c) 2025-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
# http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

---
aerial_sdk_version: 26-1-cubb
# WAR for limitation that all cells need to be configured at initial
wait_all_cell_config: 1

# Transport settings for nvIPC
transport:
  # Unique identifier for this transport instance (must be sequential starting from 0)
  - transport_id: 0
    # Array of phy_cell_id mapped to this transport instance
    phy_cells: [0, 1, 2, 3]
    # Transport type: only "shm" for shared memory is supported
    type: shm
    # Shared memory transport configuration
    shm_config:
      # Prefix used for shared memory object names (e.g., /dev/shm/${prefix}_*)
      prefix: nvipc
      # CUDA device ID for GPU memory operations. The cpu_data and cpu_large memory pools will be registered to host pinned memory when cuda_device_id >= 0
      cuda_device_id: 0
      # Length of the ring buffer queue for message passing between processes
      ring_len: 8192
      # Memory pool size configuration for different buffer types
      mempool_size:
        # CPU message buffer pool: used for small control messages
        cpu_msg:
          # Size of each buffer in bytes
          buf_size: 15000
          # Number of buffers in the pool
          pool_len: 4096
        # CPU data buffer pool: used for larger data payloads on CPU
        cpu_data:
          # Size of each buffer in bytes
          buf_size: 576000
          # Number of buffers in the pool
          pool_len: 1024
        # CPU large buffer pool: used for very large data payloads on CPU
        cpu_large:
          # Size of each buffer in bytes
          buf_size: 4096000
          # Number of buffers in the pool
          pool_len: 64
        # CUDA/GPU data buffer pool: used for GPU-accessible data buffers
        cuda_data:
          # Size of each buffer in bytes
          buf_size: 307200
          # Number of buffers in the pool (0 means pool is disabled)
          pool_len: 0
    # PCAP logging and debug configurations
    app_config:
      grpc_forward: 0 # Forward nvipc message to a "<prefix>_fw" queue for engineering debug
      debug_timing: 0 # Saving full timestamps of nvipc message states for engineering debug
      pcap_enable: 0 # Enable PCAP logging
      pcap_sync_save: 0 # 0 - Asynchronized saving packets in SHM caching thread; 1 - Synchronized saving packets in caller thread
      pcap_shm_caching_cpu_core: 18 # CPU core of pcap shared memory caching thread
      pcap_file_saving_cpu_core: 18 # CPU core of pcap file saving thread
      pcap_cache_size_bits: 29 # 2^29 = 512MB, size of /dev/shm/${prefix}_pcap
      pcap_file_size_bits: 31 # 2^31 = 2GB, max size of /var/log/aerial/${prefix}_pcap. Requires pcap_file_size_bits > pcap_cache_size_bits.
      pcap_max_data_size: 8000 # Max DL/UL FAPI data size to capture reduce pcap size.
      msg_filter: [] # Example: [0x02, 0x03, 0x04, 0x05, 0x06, 0x07, 0x80, 0x81, 0x82, 0x83, 0x84, 0x85]
      cell_filter: [] # Example: [0, 1, 2, 3]

  # Unique identifier for this transport instance (must be sequential starting from 0)
  - transport_id: 1
    # Array of phy_cell_id mapped to this transport instance
    phy_cells: [4, 5, 6, 7]
    # Transport type: only "shm" for shared memory is supported
    type: shm
    # Shared memory transport configuration
    shm_config:
      # Prefix used for shared memory object names (e.g., /dev/shm/${prefix}_*)
      prefix: nvipc1
      # CUDA device ID for GPU memory operations. The cpu_data and cpu_large memory pools will be registered to host pinned memory when cuda_device_id >= 0
      cuda_device_id: 0
      # Length of the ring buffer queue for message passing between processes
      ring_len: 8192
      # Memory pool size configuration for different buffer types
      mempool_size:
        # CPU message buffer pool: used for small control messages
        cpu_msg:
          # Size of each buffer in bytes
          buf_size: 15000
          # Number of buffers in the pool
          pool_len: 4096
        # CPU data buffer pool: used for larger data payloads on CPU
        cpu_data:
          # Size of each buffer in bytes
          buf_size: 576000
          # Number of buffers in the pool
          pool_len: 1024
        # CPU large buffer pool: used for very large data payloads on CPU
        cpu_large:
          # Size of each buffer in bytes
          buf_size: 4096000
          # Number of buffers in the pool
          pool_len: 64
        # CUDA/GPU data buffer pool: used for GPU-accessible data buffers
        cuda_data:
          # Size of each buffer in bytes
          buf_size: 307200
          # Number of buffers in the pool (0 means pool is disabled)
          pool_len: 0
    # PCAP logging and debug configurations
    app_config:
      grpc_forward: 0 # Forward nvipc message to a "<prefix>_fw" queue for engineering debug
      debug_timing: 0 # Saving full timestamps of nvipc message states for engineering debug
      pcap_enable: 0 # Enable PCAP logging
      pcap_sync_save: 0 # 0 - Asynchronized saving packets in SHM caching thread; 1 - Synchronized saving packets in caller thread
      pcap_shm_caching_cpu_core: 22 # CPU core of pcap shared memory caching thread
      pcap_file_saving_cpu_core: 22 # CPU core of pcap file saving thread
      pcap_cache_size_bits: 29 # 2^29 = 512MB, size of /dev/shm/${prefix}_pcap
      pcap_file_size_bits: 31 # 2^31 = 2GB, max size of /var/log/aerial/${prefix}_pcap. Requires pcap_file_size_bits > pcap_cache_size_bits.
      pcap_max_data_size: 8000 # Max DL/UL FAPI data size to capture reduce pcap size.
      msg_filter: [] # Example: [0x02, 0x03, 0x04, 0x05, 0x06, 0x07, 0x80, 0x81, 0x82, 0x83, 0x84, 0x85]
      cell_filter: [] # Example: [0, 1, 2, 3]

  # Unique identifier for this transport instance (must be sequential starting from 0)
  - transport_id: 2
    # Array of phy_cell_id mapped to this transport instance
    phy_cells: [8, 9, 10, 11]
    # Transport type: only "shm" for shared memory is supported
    type: shm
    # Shared memory transport configuration
    shm_config:
      # Prefix used for shared memory object names (e.g., /dev/shm/${prefix}_*)
      prefix: nvipc2
      # CUDA device ID for GPU memory operations. The cpu_data and cpu_large memory pools will be registered to host pinned memory when cuda_device_id >= 0
      cuda_device_id: 0
      # Length of the ring buffer queue for message passing between processes
      ring_len: 8192
      # Memory pool size configuration for different buffer types
      mempool_size:
        # CPU message buffer pool: used for small control messages
        cpu_msg:
          # Size of each buffer in bytes
          buf_size: 15000
          # Number of buffers in the pool
          pool_len: 4096
        # CPU data buffer pool: used for larger data payloads on CPU
        cpu_data:
          # Size of each buffer in bytes
          buf_size: 576000
          # Number of buffers in the pool
          pool_len: 1024
        # CPU large buffer pool: used for very large data payloads on CPU
        cpu_large:
          # Size of each buffer in bytes
          buf_size: 4096000
          # Number of buffers in the pool
          pool_len: 64
        # CUDA/GPU data buffer pool: used for GPU-accessible data buffers
        cuda_data:
          # Size of each buffer in bytes
          buf_size: 307200
          # Number of buffers in the pool (0 means pool is disabled)
          pool_len: 0
    # PCAP logging and debug configurations
    app_config:
      grpc_forward: 0 # Forward nvipc message to a "<prefix>_fw" queue for engineering debug
      debug_timing: 0 # Saving full timestamps of nvipc message states for engineering debug
      pcap_enable: 0 # Enable PCAP logging
      pcap_sync_save: 0 # 0 - Asynchronized saving packets in SHM caching thread; 1 - Synchronized saving packets in caller thread
      pcap_shm_caching_cpu_core: 26 # CPU core of pcap shared memory caching thread
      pcap_file_saving_cpu_core: 26 # CPU core of pcap file saving thread
      pcap_cache_size_bits: 29 # 2^29 = 512MB, size of /dev/shm/${prefix}_pcap
      pcap_file_size_bits: 31 # 2^31 = 2GB, max size of /var/log/aerial/${prefix}_pcap. Requires pcap_file_size_bits > pcap_cache_size_bits.
      pcap_max_data_size: 8000 # Max DL/UL FAPI data size to capture reduce pcap size.
      msg_filter: [] # Example: [0x02, 0x03, 0x04, 0x05, 0x06, 0x07, 0x80, 0x81, 0x82, 0x83, 0x84, 0x85]
      cell_filter: [] # Example: [0, 1, 2, 3]

  # Unique identifier for this transport instance (must be sequential starting from 0)
  - transport_id: 3
    # Array of phy_cell_id mapped to this transport instance
    phy_cells: [12, 13, 14, 15]
    # Transport type: only "shm" for shared memory is supported
    type: shm
    # Shared memory transport configuration
    shm_config:
      # Prefix used for shared memory object names (e.g., /dev/shm/${prefix}_*)
      prefix: nvipc3
      # CUDA device ID for GPU memory operations. The cpu_data and cpu_large memory pools will be registered to host pinned memory when cuda_device_id >= 0
      cuda_device_id: 0
      # Length of the ring buffer queue for message passing between processes
      ring_len: 8192
      # Memory pool size configuration for different buffer types
      mempool_size:
        # CPU message buffer pool: used for small control messages
        cpu_msg:
          # Size of each buffer in bytes
          buf_size: 15000
          # Number of buffers in the pool
          pool_len: 4096
        # CPU data buffer pool: used for larger data payloads on CPU
        cpu_data:
          # Size of each buffer in bytes
          buf_size: 576000
          # Number of buffers in the pool
          pool_len: 1024
        # CPU large buffer pool: used for very large data payloads on CPU
        cpu_large:
          # Size of each buffer in bytes
          buf_size: 4096000
          # Number of buffers in the pool
          pool_len: 64
        # CUDA/GPU data buffer pool: used for GPU-accessible data buffers
        cuda_data:
          # Size of each buffer in bytes
          buf_size: 307200
          # Number of buffers in the pool (0 means pool is disabled)
          pool_len: 0
    # PCAP logging and debug configurations
    app_config:
      grpc_forward: 0 # Forward nvipc message to a "<prefix>_fw" queue for engineering debug
      debug_timing: 0 # Saving full timestamps of nvipc message states for engineering debug
      pcap_enable: 0 # Enable PCAP logging
      pcap_sync_save: 0 # 0 - Asynchronized saving packets in SHM caching thread; 1 - Synchronized saving packets in caller thread
      pcap_shm_caching_cpu_core: 30 # CPU core of pcap shared memory caching thread
      pcap_file_saving_cpu_core: 30 # CPU core of pcap file saving thread
      pcap_cache_size_bits: 29 # 2^29 = 512MB, size of /dev/shm/${prefix}_pcap
      pcap_file_size_bits: 31 # 2^31 = 2GB, max size of /var/log/aerial/${prefix}_pcap. Requires pcap_file_size_bits > pcap_cache_size_bits.
      pcap_max_data_size: 8000 # Max DL/UL FAPI data size to capture reduce pcap size.
      msg_filter: [] # Example: [0x02, 0x03, 0x04, 0x05, 0x06, 0x07, 0x80, 0x81, 0x82, 0x83, 0x84, 0x85]
      cell_filter: [] # Example: [0, 1, 2, 3]

  # Unique identifier for this transport instance (must be sequential starting from 0)
  - transport_id: 4
    # Array of phy_cell_id mapped to this transport instance
    phy_cells: [16, 17, 18, 19]
    # Transport type: only "shm" for shared memory is supported
    type: shm
    # Shared memory transport configuration
    shm_config:
      # Prefix used for shared memory object names (e.g., /dev/shm/${prefix}_*)
      prefix: nvipc4
      # CUDA device ID for GPU memory operations. The cpu_data and cpu_large memory pools will be registered to host pinned memory when cuda_device_id >= 0
      cuda_device_id: 0
      # Length of the ring buffer queue for message passing between processes
      ring_len: 8192
      # Memory pool size configuration for different buffer types
      mempool_size:
        # CPU message buffer pool: used for small control messages
        cpu_msg:
          # Size of each buffer in bytes
          buf_size: 15000
          # Number of buffers in the pool
          pool_len: 4096
        # CPU data buffer pool: used for larger data payloads on CPU
        cpu_data:
          # Size of each buffer in bytes
          buf_size: 576000
          # Number of buffers in the pool
          pool_len: 1024
        # CPU large buffer pool: used for very large data payloads on CPU
        cpu_large:
          # Size of each buffer in bytes
          buf_size: 4096000
          # Number of buffers in the pool
          pool_len: 64
        # CUDA/GPU data buffer pool: used for GPU-accessible data buffers
        cuda_data:
          # Size of each buffer in bytes
          buf_size: 307200
          # Number of buffers in the pool (0 means pool is disabled)
          pool_len: 0
    # PCAP logging and debug configurations
    app_config:
      grpc_forward: 0 # Forward nvipc message to a "<prefix>_fw" queue for engineering debug
      debug_timing: 0 # Saving full timestamps of nvipc message states for engineering debug
      pcap_enable: 0 # Enable PCAP logging
      pcap_sync_save: 0 # 0 - Asynchronized saving packets in SHM caching thread; 1 - Synchronized saving packets in caller thread
      pcap_shm_caching_cpu_core: 34 # CPU core of pcap shared memory caching thread
      pcap_file_saving_cpu_core: 34 # CPU core of pcap file saving thread
      pcap_cache_size_bits: 29 # 2^29 = 512MB, size of /dev/shm/${prefix}_pcap
      pcap_file_size_bits: 31 # 2^31 = 2GB, max size of /var/log/aerial/${prefix}_pcap. Requires pcap_file_size_bits > pcap_cache_size_bits.
      pcap_max_data_size: 8000 # Max DL/UL FAPI data size to capture reduce pcap size.
      msg_filter: [] # Example: [0x02, 0x03, 0x04, 0x05, 0x06, 0x07, 0x80, 0x81, 0x82, 0x83, 0x84, 0x85]
      cell_filter: [] # Example: [0, 1, 2, 3]

...
